/*
 * Platform: Coding Ninjas (Code360)
 * Problem: Pair Sum
 * URL: https://www.naukri.com/code360/problems/pair-sum_697295
 * Language: Java (17)
 * Difficulty: Easy
 * Topics: Uncategorized
 * Runtime: N/A
 * Memory: N/A
 * Synced: 2026-09-19T07:30:37.262Z
 */

while (left <= right && arr[left] == start) {
                    countLeft++;
                    left++;
                }
 
                while (left <= right && arr[right] == end) {
                    countRight++;
                    right--;
                }
 
                // Same elements case
                if (start == end) {
                    int totalPairs = (countLeft * (countLeft - 1)) / 2;
 
                    for (int i = 0; i < totalPairs; i++) {
                        ans.add(new int[]{start, end});
                    }
                }
                else {
                    int totalPairs = countLeft * countRight;
 
                    for (int i = 0; i < totalPairs; i++) {
                        ans.add(new int[]{start, end});
                    }
                }
            }
 
            else if (sum < s) {
                left++;
            }
 
            else {
                right--;
            }
        }
 
        return ans;
    }
}
