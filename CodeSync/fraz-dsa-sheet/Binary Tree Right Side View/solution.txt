/*
 * Platform: LeetCode
 * Problem: Binary Tree Right Side View
 * URL: https://leetcode.com/problems/binary-tree-right-side-view/submissions/2139072992/?envType=study-plan-v2&envId=leetcode-75
 * Language: LeetCode 75
 * Difficulty: Unknown
 * Topics: Uncategorized
 * Runtime: 1 ms
 * Memory: 43.38 MB
 * Synced: 2026-09-12T07:51:24.774Z
 */

class Solution {
    public List<Integer> rightSideView(TreeNode root) {
        List<Integer> ans = new ArrayList<>();
        // Empty tree
        if (root == null) return ans;
        
        Queue<TreeNode> queue = new LinkedList<>();
        queue.offer(root);

        while (!queue.isEmpty()) {

            int size = queue.size();

            // Current level ke saare nodes process karo
            for (int i = 0; i < size; i++) {

                TreeNode node = queue.poll();

                // Current level ka last node
                if (i == size - 1) {
                    ans.add(node.val);
                }
