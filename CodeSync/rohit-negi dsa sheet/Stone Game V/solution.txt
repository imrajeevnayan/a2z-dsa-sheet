/*
 * Platform: LeetCode
 * Problem: Stone Game V
 * URL: https://leetcode.com/problems/stone-game-v/submissions/2109740530/?envType=daily-question&envId=2026-08-17
 * Language: Daily Question
 * Difficulty: Hard
 * Topics: Array, Math, Dynamic Programming, Game Theory
 * Runtime: 0 ms
 * Memory: 46.33 MB
 * Synced: 2026-08-17T05:54:37.375Z
 */

if (leftSum < rightSum) {
                    int rightSum = prefix[right + 1] - prefix[mid + 1];

                for (int mid = left; mid < right; mid++) {
                    int leftSum = prefix[mid + 1] - prefix[left];

                int right = left + len - 1;
            for (int left = 0; left + len <= n; left++) {
        for (int len = 2; len <= n; len++) {

        int[][] dp = new int[n][n];

        }
            prefix[i + 1] = prefix[i] + stoneValue[i];
        for (int i = 0; i < n; i++) {
        int[] prefix = new int[n + 1];

        int n = stoneValue.length;
    public int stoneGameV(int[] stoneValue) {
class Solution {
