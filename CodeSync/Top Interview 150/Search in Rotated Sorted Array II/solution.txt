/*
 * Platform: LeetCode
 * Problem: Search in Rotated Sorted Array II
 * URL: https://leetcode.com/problems/search-in-rotated-sorted-array-ii/submissions/2128496580/
 * Language: Problem List
 * Difficulty: Medium
 * Topics: Array, Binary Search
 * Runtime: 0 ms
 * Memory: 45.24 MB
 * Synced: 2026-09-02T13:53:07.469Z
 */

}
            // Left half sorted
            else if (nums[low] <= nums[mid]) {
                if (nums[low] <= target && target < nums[mid]) high = mid - 1;
                else low = mid + 1;
            }
            // Right half sorted
            else {
                if (nums[mid] < target && target <= nums[high]) low = mid + 1;
               else high = mid - 1;
            }
        }
        return ans != -1;
                low++;
                high--;
            // Duplicate ambiguity
            if (nums[low] == nums[mid] && nums[mid] == nums[high]) {
            }
                ans = mid;
                break;
            if (nums[mid] == target) {
