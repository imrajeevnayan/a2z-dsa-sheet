/*
 * Platform: LeetCode
 * Problem: test case :-
 * URL: https://leetcode.com/problems/house-robber-ii/
 * Language: Problem List
 * Difficulty: Medium
 * Topics: Array, Dynamic Programming
 * Runtime: 0 ms
 * Memory: N/A
 * Synced: 2026-07-20T13:56:14.656Z
 */

int rob = nums[start] + helper(nums, start + 2, end, memo);
    int skip = helper(nums, start + 1, end, memo);
  return memo[start] = Math.max(rob, skip);
   }
   public int rob(int[] nums) {
    int n = nums.length;
    if (n == 1) return nums[0];

    int[] dp = new int[n];
    int[] dp = new int[n];
    Arrays.fill(dp, -1);
    Arrays.fill(dp, -1);

    int robFirst = helper(nums, 0, n - 2,dp);
    int robLast = helper(nums, 1, n - 1, memo2);
    return Math.max(robFirst, robLast);
 }

}
    if (memo[start] != -1) return memo[start];
    if (start > end) return 0;
