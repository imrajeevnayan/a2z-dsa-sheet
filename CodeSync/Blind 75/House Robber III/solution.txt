/*
 * Platform: LeetCode
 * Problem: House Robber III
 * URL: https://leetcode.com/problems/house-robber-iii/
 * Language: Problem List
 * Difficulty: Medium
 * Topics: Dynamic Programming, Tree, Depth-First Search, Binary Tree, DP on Trees
 * Runtime: N/A
 * Memory: N/A
 * Synced: 2026-09-23T12:11:19.519Z
 */

class Solution {
    int solve(int i, int[] nums, int end, int[] dp) {
        if (i > end) return 0;
        if (dp[i] != -1) return dp[i];
        int take = nums[i] + solve(i + 2, nums, end, dp);
        int skip = solve(i + 1, nums, end, dp);
        return dp[i] = Math.max(take, skip);
    }
    public int rob(int[] nums) {
        int n = nums.length;
        if (n == 1) return nums[0];
        int[] dp = new int[n];
        Arrays.fill(dp, -1);
        int case1 = solve(0, nums, n - 2, dp);

        Arrays.fill(dp, -1); //reset
        int case2 = solve(1, nums, n - 1, dp);
        return Math.max(case1, case2);
    }
}
