/*
 * Platform: LeetCode
 * Problem: Maximum Swap
 * URL: https://leetcode.com/problems/maximum-swap/submissions/2051009094/
 * Language: Problem List
 * Difficulty: Medium
 * Topics: Math, Greedy
 * Runtime: 0 ms
 * Memory: 42.28 MB
 * Synced: 2026-06-30T09:08:52.025Z
 */

for (int i = 0; i < n; i++) {
            lastIdx[digits[i] - '0'] = i;
        }
        
        for (int i = 0; i < n; i++) {
            for (int d = 9; d > digits[i] - '0'; d--) {
                if (lastIdx[d] > i) {
                    char temp = digits[i];
                    digits[i] = digits[lastIdx[d]];
                    digits[lastIdx[d]] = temp;
                    return Integer.parseInt(new String(digits));
                }
        
        int[] lastIdx = new int[10];
    public int maximumSwap(int num) {
        char[] digits = String.valueOf(num).toCharArray();
        int n = digits.length;
class Solution {
