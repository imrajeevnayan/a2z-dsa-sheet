/*
 * Platform: LeetCode
 * Problem: Jump Game
 * URL: https://leetcode.com/problems/jump-game/?envType=problem-list-v2&envId=dynamic-programming
 * Language: Dynamic Programming
 * Difficulty: Medium
 * Topics: Array, Dynamic Programming, Greedy
 * Runtime: N/A
 * Memory: N/A
 * Synced: 2026-07-19T20:25:12.831Z
 */

if (i == nums.length - 1) return true;
        if (i >= nums.length) return false;
        if (nums[i] == 0) return false;

        if (dp[i] != -1)return d[[i] == 1;
        

        boolean reAns = false;
        for (int jump = 1; jump <= nums[i]; jump++) {
            if (solve(nums, i + jump, memo)) {
        // Base Cases--
    private boolean solve(int[] nums, int i, int[] dp) {
    }
        return solve(nums, 0,dp);
        Arrays.fill(dp, -1);
        int[] dp = new int[nums.length];
    public boolean canJump(int[] nums) {
class Solution {
                reAns = true;
                break; // Optimization: stop if we found one valid path
            }
        }
