/*
 * Platform: LeetCode
 * Problem: Cinema Seat Allocation
 * URL: https://leetcode.com/problems/cinema-seat-allocation/submissions/2112584085/?envType=daily-question&envId=2026-08-19
 * Language: Daily Question
 * Difficulty: Medium
 * Topics: Array, Hash Table, Greedy, Bit Manipulation
 * Runtime: 0 ms
 * Memory: 53.18 MB
 * Synced: 2026-08-19T10:49:03.927Z
 */

for (int mask : rows.values()) {
            boolean l = (mask & left) == 0;
            boolean r = (mask & right) == 0;

        int right = 0b1111000000;
        int middle = 0b11110000;
        int left = 0b00111100;

        int result = (n - rows.size()) * 2;
            if (l && r) {
                result += 2;
            } else if (l || r || (mask & middle) == 0) {
                result++;
            }
        }

        return result;
    }
}
