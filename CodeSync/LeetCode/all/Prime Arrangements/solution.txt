/*
 * Platform: LeetCode
 * Problem: Prime Arrangements
 * URL: https://leetcode.com/problems/prime-arrangements/submissions/2143450302/
 * Language: Problem List
 * Difficulty: Easy
 * Topics: Math, Primality Test, Sieve Theory, Prime Number Sieve
 * Runtime: 0 ms
 * Memory: 42.05 MB
 * Synced: 2026-09-16T08:22:32.914Z
 */

// Count primes
            }
        }
                    isPrime[j] = false;
                }
                for (int j = i * i; j <= n; j += i) {
        int primeCount = 0;
        for (int i = 2; i <= n; i++) {
            if (isPrime[i]) primeCount++;
        }
        int nonPrimeCount = n - primeCount;

        long primeWays = factorial(primeCount);
        long nonPrimeWays = factorial(nonPrimeCount);

        return (int)((primeWays * nonPrimeWays) % MOD);
            if (isPrime[i]) {
        for (int i = 2; i * i <= n; i++) {

        // Sieve of Eratosthenes
        isPrime[0] = isPrime[1] = false;
        boolean[] isPrime = new boolean[n + 1];
        Arrays.fill(isPrime, true);
    public int numPrimeArrangements(int n) {
class Solution {
   static final long MOD = 1000000007;
