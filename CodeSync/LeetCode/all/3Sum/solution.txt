/*
 * Platform: LeetCode
 * Problem: 3Sum
 * URL: https://leetcode.com/problems/3sum/submissions/2067368228/
 * Language: Problem List
 * Difficulty: Medium
 * Topics: Array, Two Pointers, Sorting
 * Runtime: 35 ms
 * Memory: 59.00 MB
 * Synced: 2026-07-14T13:08:13.002Z
 */

result.add(Arrays.asList(nums[i], nums[left], nums[right]));
                    while (left < right && nums[left] == nums[left + 1]) left++; // Skip left duplicates
                if (sum == 0) {
            while (left < right) {
                int sum = nums[i] + nums[left] + nums[right];

            int left = i + 1, right = nums.length - 1;
            if (i > 0 && nums[i] == nums[i - 1]) continue; 
        for (int i = 0; i < nums.length - 2; i++) {
                    while (left < right && nums[right] == nums[right - 1]) right--; // Skip right duplicates
                    left++;
                    right--;
                } else if (sum < 0)  left++;
                 else  right--;  
            }

        List<List<Integer>> result = new ArrayList<>();
        Arrays.sort(nums);
    public List<List<Integer>> threeSum(int[] nums) {
class Solution {
