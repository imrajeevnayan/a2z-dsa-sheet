/*
 * Platform: LeetCode
 * Problem: Find the Longest Equal Subarray
 * URL: https://leetcode.com/problems/find-the-longest-equal-subarray/
 * Language: Problem List
 * Difficulty: Medium
 * Topics: Array, Hash Table, Binary Search, Sliding Window
 * Runtime: N/A
 * Memory: N/A
 * Synced: 2026-09-15T12:30:01.998Z
 */

// MaxFreq update karo: Kya abhi wala number sabse zyada frequent ho gaya?
            maxFreq = Math.max(maxFreq, map.get(currentNum));
            
            // 3. Check karo: Kya window valid hai?
            // Valid condition: (Window Size - MaxFreq) <= k
            // Iska matlab: Jo numbers "most frequent" nahi hain, unhe delete karne ke liye k permissions chahiye
            int windowSize = right - left + 1;
            
            map.put(currentNum, map.getOrDefault(currentNum, 0) + 1);
            // getOrDefault: Agar number pehle se hai toh uski value lo, nahi toh 0 lo
            int currentNum = nums.get(right);
            
            // Map mein current number ki frequency badhao
        // 2. Right pointer se window ko expand karte hain
        for (int right = 0; right < nums.size(); right++) {
        
        
        int left = 0; maxFreq = 0;    // Current window mein kisi bhi number ki sabse zyada count
        Map<Integer, Integer> map = new HashMap<>();
    public int longestEqualSubarray(List<Integer> nums, int k) {
class Solution {
            if (windowSize - maxFreq > k) {
                // Agar deletions k se zyada ho rahe hain, toh window shrink karo (left badhao)
                
                int leftNum = nums.get(left);
