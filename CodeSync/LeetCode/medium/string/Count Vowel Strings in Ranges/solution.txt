/*
 * Platform: LeetCode
 * Problem: Count Vowel Strings in Ranges
 * URL: https://leetcode.com/problems/count-vowel-strings-in-ranges/?envType=problem-list-v2&envId=prefix-sum
 * Language: Prefix Sum
 * Difficulty: Medium
 * Topics: Array, String, Prefix Sum
 * Runtime: 0 ms
 * Memory: N/A
 * Synced: 2026-08-23T06:49:07.316Z
 */

int[] ans = new int[queries.length];
            }
        }
                prefix[i + 1]++;
                isVowel(word.charAt(word.length() - 1))) {
            if (isVowel(word.charAt(0)) &&
            String word = words[i];

            prefix[i + 1] = prefix[i];
        for (int i = 0; i < n; i++) {

        int[] prefix = new int[n + 1];
        for (int i = 0; i < queries.length; i++) {
            int left = queries[i][0];
            int right = queries[i][1];
