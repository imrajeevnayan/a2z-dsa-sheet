/*
 * Platform: LeetCode
 * Problem: Valid Binary Strings With Cost Limit
 * URL: https://leetcode.com/contest/weekly-contest-505/problems/valid-binary-strings-with-cost-limit/submissions/2061780959/
 * Language: Weekly Contest 505
 * Difficulty: Medium
 * Topics: Uncategorized
 * Runtime: 0 ms
 * Memory: 0.00 MB
 * Synced: 2026-07-09T13:42:04.218Z
 */

class Solution {
    public List<String> generateValidStrings(int n, int k) {
        List<String> ans = new ArrayList<>();
        StringBuilder sb = new StringBuilder();
        dfs(0, n, k, 0, false, sb, ans);
        return ans;
    }


    private void dfs(int idx, int n, int k, int cost,
                     boolean prevOne, StringBuilder sb,
                     List<String> ans) {


        if (idx == n) {
            ans.add(sb.toString());
            return;
        }


        // Place '0'
        sb.append('0');
        dfs(idx + 1, n, k, cost, false, sb, ans);
        sb.deleteCharAt(sb.length() - 1);


        // Place '1'
        if (!prevOne && cost + idx <= k) {
            sb.append('1');
            dfs(idx + 1, n, k, cost + idx, true, sb, ans);
            sb.deleteCharAt(sb.length() - 1);
        }
    }
}
3
1
1
0
["000","010","100"]
["0","1"]
