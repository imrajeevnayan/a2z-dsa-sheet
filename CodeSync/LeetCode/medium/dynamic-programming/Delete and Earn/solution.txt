/*
 * Platform: LeetCode
 * Problem: Delete and Earn
 * URL: https://leetcode.com/problems/delete-and-earn/
 * Language: Problem List
 * Difficulty: Medium
 * Topics: Array, Hash Table, Dynamic Programming
 * Runtime: N/A
 * Memory: N/A
 * Synced: 2026-09-23T11:27:26.668Z
 */

for (int num : nums) {
            points[num] += num;
        }

        dp = new int[max + 1];

        Arrays.fill(dp, -1);
        return solve(max);
    }

    private int solve(int i) {
        // Base case
        if (i <= 0) return 0;
        if (dp[i] != -1) return dp[i];

        int take = points[i] + solve(i - 2);
        int skip = solve(i - 1);
        return dp[i] = Math.max(take, skip);
    }
}
