/*
 * Platform: LeetCode
 * Problem: Delete and Earn
 * URL: https://leetcode.com/problems/delete-and-earn/?envType=problem-list-v2&envId=dynamic-programming
 * Language: Dynamic Programming
 * Difficulty: Medium
 * Topics: Array, Hash Table, Dynamic Programming
 * Runtime: N/A
 * Memory: N/A
 * Synced: 2026-09-16T12:06:25.322Z
 */

return solve(max);
    }

    private int solve(int i) {
        // Base case
        if (i <= 0) return 0;
        if (dp[i] != -1) return dp[i];

        int take = points[i] + solve(i - 2);
        int skip = solve(i - 1);
        return dp[i] = Math.max(take, skip);
    }
}
        Arrays.fill(dp, -1);
        // -1 means not calculated yet

        dp = new int[max + 1];

        }
            points[num] += num;
        for (int num : nums) {
