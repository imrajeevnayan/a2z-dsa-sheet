/*
 * Platform: LeetCode
 * Problem: Find if Path Exists in Graph
 * URL: https://leetcode.com/problems/find-if-path-exists-in-graph/submissions/2135051154/
 * Language: Problem List
 * Difficulty: Easy
 * Topics: Depth-First Search, Breadth-First Search, Union-Find, Graph Theory
 * Runtime: 0 ms
 * Memory: N/A
 * Synced: 2026-09-08T11:47:09.314Z
 */

// Visit neighbours
        for (int next : graph.get(node)) {

            if (!visited[next]) {
                if (dfs( next, destination, graph, visited)) return true;  
            }
        }

        return false;
    }
}


        visited[node] = true;
        // Mark visited
        if (node == destination)return true;
        // Destination found
    private boolean dfs( int node,int destination,List<List<Integer>> graph, boolean[] visited) {
    }
        return dfs(source, destination, graph, visited );
