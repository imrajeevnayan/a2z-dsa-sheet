/*
 * Platform: LeetCode
 * Problem: Code
 * URL: https://leetcode.com/problems/intersection-of-two-arrays/submissions/2117255312/?envType=problem-list-v2&envId=da2ht4id
 * Language: 60 questions to solve (Copy-9xpbvcmc)
 * Difficulty: Easy
 * Topics: Array, Hash Table, Two Pointers, Binary Search, Sorting
 * Runtime: 0 ms
 * Memory: 45.15 MB
 * Synced: 2026-08-23T11:11:08.048Z
 */

for (int num : nums2) {
            if (set.contains(num)) result.add(num);
 
        }

        int[] ans = new int[result.size()];
        int i = 0;
        for (int num : result) ans[i++] = num;
        return ans;
    }

}

        Set<Integer> result = new HashSet<>();
        for (int num : nums1)  set.add(num);
        Set<Integer> set = new HashSet<>();
