/*
 * Platform: LeetCode
 * Problem: Minimum Remove to Make Valid Parentheses
 * URL: https://leetcode.com/problems/minimum-remove-to-make-valid-parentheses/submissions/2151939220/
 * Language: Problem List
 * Difficulty: Medium
 * Topics: String, Stack
 * Runtime: 0 ms
 * Memory: 47.42 MB
 * Synced: 2026-09-24T11:53:22.761Z
 */

if (!st.isEmpty()) st.pop();
                else  set.add(i);

            }
        }

        // Unmatched '('
        while (!st.isEmpty()) {
            set.add(st.pop());
        }
        StringBuilder result = new StringBuilder();

        for (int i = 0; i < s.length(); i++) {
            else if (ch == ')') {
        

            if (ch == '(')  st.push(i);
            char ch = s.charAt(i);

        for (int i = 0; i < s.length(); i++) {
        Set<Integer> set = new HashSet<>();
        Stack<Integer> st = new Stack<>();

    public String minRemoveToMakeValid(String s) {
class Solution {
