/*
 * Platform: LeetCode
 * Problem: Remove All Adjacent Duplicates In String
 * URL: https://leetcode.com/problems/remove-all-adjacent-duplicates-in-string/submissions/2152821149/
 * Language: Problem List
 * Difficulty: Easy
 * Topics: String, Stack
 * Runtime: 0 ms
 * Memory: 46.96 MB
 * Synced: 2026-09-25T09:10:25.899Z
 */

class Solution {
    public String removeDuplicates(String s) {
        Stack<Character>st=new Stack<>();
        for(char ch: s.toCharArray()){
            if(!st.isEmpty() && st.peek()==ch) st.pop();
            else st.push(ch);
        }
        StringBuilder sb=new StringBuilder();
        while(!st.isEmpty()){
            sb.append(st.pop());
        }
       return sb.reverse().toString(); 
    }
}
