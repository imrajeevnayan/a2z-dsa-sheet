/*
 * Platform: LeetCode
 * Problem: Jump Game
 * URL: https://leetcode.com/problems/jump-game/submissions/2073885959/?envType=problem-list-v2&envId=dynamic-programming
 * Language: Dynamic Programming
 * Difficulty: Medium
 * Topics: Array, Dynamic Programming, Greedy
 * Runtime: 0 ms
 * Memory: 47.83 MB
 * Synced: 2026-07-19T20:26:44.352Z
 */

if (i == nums.length - 1) return true;
        if (i >= nums.length) return false;
        if (nums[i] == 0) return false;

        if (dp[i] != -1)return dp[i] == 1;

        boolean reAns = false;
        for (int jump = 1; jump <= nums[i]; jump++) {
            if (solve(nums, i + jump, dp)) {
        // Base Cases--
    private boolean solve(int[] nums, int i, int[] dp) {
    }
                break; 
            }
        }
        dp[i] = reAns ? 1 : 0;
        return reAns;
                reAns = true;
    }
}
