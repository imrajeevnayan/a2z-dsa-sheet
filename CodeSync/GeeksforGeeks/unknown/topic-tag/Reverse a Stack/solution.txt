/*
 * Platform: GeeksforGeeks
 * Problem: Reverse a Stack
 * URL: https://www.geeksforgeeks.org/problems/reverse-a-stack/1
 * Language: Java (21)
 * Difficulty: Unknown
 * Topics: Expected Complexities, Topic Tags, Recursion, Stack, Related Articles, Reverse A Stack Using Recursion
 * Runtime: N/A
 * Memory: N/A
 * Synced: 2026-07-18T18:24:54.787Z
 */

class Solution {
    public static void reverseStack(Stack<Integer> st) {
        if (st.isEmpty())  return;
        int top = st.pop();
        reverseStack(st);
        insertAtBottom(st, top);
    }
    private static void insertAtBottom(Stack<Integer> st, int x) {
        if (st.isEmpty()) {
            st.push(x);
            return;
        }
        int top = st.pop();
        insertAtBottom(st, x);
        st.push(top);
    }
}
