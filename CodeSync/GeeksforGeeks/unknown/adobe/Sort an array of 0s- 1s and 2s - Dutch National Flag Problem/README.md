# Sort an array of 0s, 1s and 2s - Dutch National Flag Problem

- Platform: GeeksforGeeks
- Language: #include <iostream> #include <vector> #include <algorithm> using namespace std; void sort012(vector<int> &arr) { // standard sorting function sort(arr.begin(), arr.end()); } int main() { vector<int> arr = {0, 1, 2, 0, 1, 2}; sort012(arr); for (int i = 0; i < arr.size(); i++) cout << arr[i] << " "; return 0; }
- Difficulty: Unknown
- Topics: Microsoft, Amazon, Adobe, Morgan Stanley, Qualcomm, Walmart, Yatra.com, Snapdeal
- Runtime: N/A
- Memory: N/A
- Problem URL: https://www.geeksforgeeks.org/dsa/sort-an-array-of-0s-1s-and-2s/
- Synced: 2026-06-30T08:57:09.760Z

## Problem Description

Problem description was not available on the page at sync time.

## Explanation

This solution was accepted on GeeksforGeeks using #include <iostream> #include <vector> #include <algorithm> using namespace std; void sort012(vector<int> &arr) { // standard sorting function sort(arr.begin(), arr.end()); } int main() { vector<int> arr = {0, 1, 2, 0, 1, 2}; sort012(arr); for (int i = 0; i < arr.size(); i++) cout << arr[i] << " "; return 0; }. The detected topics are Microsoft, Amazon, Adobe, Morgan Stanley, Qualcomm, Walmart, Yatra.com, Snapdeal. Review the synced source file for the implementation details.
